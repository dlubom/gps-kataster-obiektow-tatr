# PBI-042 — Skończone wartości liczbowe

Data: 2026-09-19
Branch: `codex/review-remediation`
Bazowy SHA: `2daa811635b09857cf0af8c9b8f0f23b9f242bef`
Status: wykonane. Ustalenie R05.

Origin pobrano, pull ff-only bez zmian, HEAD/origin 0/0, czyste wejście.
Checkpoint PBI-041: CI validate [35447188436](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/35447188436), completed/success dla bazowego SHA.
Zależność PBI-040 dostarczona. Zakres: walidacja liczb, konwersje,
PIG/TPN/profilowanie i dodatkowa ochrona serializacji JSON. Bez zmian data/.

## Kontynuacja

PBI-042 dostarczone, kod i CI potwierdzone. Po weryfikacji niniejszego
checkpointu przez Git/origin/CI następny krok to PBI-043. Nie zaczynać
go w tej sesji.


## Reprodukcja i rozwiązanie

`uv run --frozen pytest tests/test_finite_numbers.py -q` na bazowym kodzie:
**106 failed, 6 passed**. `x_1992: .nan` nie dawało błędu domenowego;
wysokość i dokładności NaN/Inf także przechodziły. Importery przyjmowały
nie-skończone liczby z CSV/XLSX, profiler zaliczał je do numeric_count.
Regresje są fixture'ami w testach, bez zależności od lokalnych logów.

Nowy moduł `numeric.py` współdzieli kontrolę skończoności, ścieżki pól
w strukturach i parser liczb źródłowych. Walidator zgłasza NON_FINITE_NUMBER
przed obliczeniami, zachowując reguły JSON Schema. Odrzuca także int poza
reprezentacją float, bez wyjątku OverflowError. Nie wprowadza nowych progów
fizycznych. Konwersje kontrolują wejście/wynik i błędy arytmetyczne.
PIG/TPN zgłaszają niepoprawne współrzędne i wysokości ze źródłowym wierszem
oraz polem, zamiast zamieniać błędną wysokość na null. PIG zachowuje cave_only,
TPN rejected; poprawne pozostałe wiersze nadal działają. Kandydaci TPN
z nie-skończonymi liczbami blokują raport. CLI podaje czytelne błędy.
JSON katalogu, eksportu, profilowania i stagingu ma allow_nan=False.

Pierwsze testy obszaru: **133 passed**. Po rozszerzeniu o null, zero,
liczby ujemne, przecinek/spacje, bool, przepełnienie, kandydatów TPN,
ochronę istniejących raportów i samodzielny eksport: **159 passed**.
Macierz obejmuje 7 pól YAML × 3 wartości, review/dry-run, 8 pól źródłowych
× 4 zapisy × CSV/XLSX, walidację, build i eksport oraz niezmienność bajtów.

## Weryfikacja wstępna

`uv sync --frozen`: exit 0, uv.lock bez zmian. Sandbox blokował cache uv
oraz przebudowę pakietu po zmianie pyproject; uruchomienie poza sandboxem
powiodło się, bez zmiany zależności.

Pierwsza pełna bramka `uv run --frozen python scripts/verify_project.py`:
`build/verification/run-qvwvw2fx/report.json`, **exit 0, 636 passed**,
Ruff format/lint, walidacja, build i readback wszystkich 7 artefaktów.
Hash drzewa: `f6729a0008f9799f3c203b2d3b35d3734e7414e64fca080af0dd7dc3f1e03d0b`.
Hash data/ przed/po:
`8f3ca9fe15c9cd3f35a6ade5673004a529c93376f1738608cbf8a606784bb45b`.
1009 obiektów, 1003 jaskinie, 1875 pomiarów, 0 relacji, 0 błędów,
2066 znanych ostrzeżeń: 1875 MISSING_HORIZONTAL_ACCURACY,
103 MEASUREMENT_OUTSIDE_VALLEYS, 72 MEASUREMENT_DISTANCE_OUTLIER,
16 OBJECT_PREFIX_MISMATCH. Dane, ID, historia i powiązania bez zmian.
Python 3.12.13, pytest 9.0.3, Ruff 0.15.13, mutmut 3.5.0,
PyYAML 6.0.3, pyproj 3.7.2, pyshp 3.0.3.

## Niezależny review

Agent review_pbi042, tylko odczyt: brak blokujących ustaleń PBI-042,
112 testów pierwszej wersji wykonane niezależnie. Istniejący błąd SQLite
przy skończonym elevation_m=2**63 został odtworzony i zapisany jako osobny
PBI-055, bez naprawy w tej sesji. Nie-skończoność i reprezentacja skończonych
liczb przy bindingu SQLite są osobnymi problemami. PBI-055 musi być
rozstrzygnięte przed końcowym odbiorem PBI-054; najbliższy krok to PBI-043.
Nowe progi geograficzne/fizyczne i bezpośrednie użycie selektora na
niewalidowanym słowniku pozostają poza kartą; produkcyjne zapisy są
chronione walidacją wejścia.

## Mutacje — diagnostyka narzędzia

Do konfiguracji dołączono numeric.py, source_profile.py i ich testy.
Pierwsza próba zatrzymała się podczas zbierania statystyk na teście CLI
profilera uruchamiającym subprocess. Wyłączono go wyłącznie z mutmut,
jak istniejące testy CLI; pozostaje w pełnej bramce pytest.
Druga próba uruchomiła 1291 mutantów, ale wszystkie dzieci zakończyły się
SIGSEGV (-11). Nie jest to wynik pozytywny ani zabicie mutantów. Raport
macOS wskazał setproctitle → darwin_set_process_title → CoreFoundation
na stronie dziecka fork, przed wykonaniem testów. Trwa ponowienie po
inicjalizacji setproctitle w procesie rodzica, bez zmiany zależności.


Delta review: brak nowych blockerów, niezależnie **159 passed**.
CLI assign_id z nan oraz TPN z NaN w kandydacie PIG: exit 1, czytelne
pole/pliki, bez tracebacku, raportów ani zapisów finalnych. Korektę granicy
ujemnych int w karcie PBI-055 uwzględniono (`-(2**63)-1` poza zakresem,
`-2**63` na dopuszczalnej granicy SQLite INTEGER).


Sama inicjalizacja setproctitle w rodzicu nie rozwiązała awarii. Test
kontrolny z pominięciem wyłącznie kosmetycznej zmiany nazwy procesu dał
zabitego mutanta zamiast SIGSEGV. Powtarzamy całą kampanię poleceniem:

```bash
uv run --frozen python -c 'import mutmut.__main__ as m; m.setproctitle = lambda *_: None; m.cli()' run --max-children 2 \
  'gps_kataster_obiektow_tatr.numeric*' \
  'gps_kataster_obiektow_tatr.coordinates*' \
  'gps_kataster_obiektow_tatr.validator.x_validate_dataset*' \
  'gps_kataster_obiektow_tatr.validator.x_validate_record_schemas*' \
  'gps_kataster_obiektow_tatr.validator.x__validate_object_measurements*' \
  'gps_kataster_obiektow_tatr.validator.x__validate_prefix_matches_best_measurement*' \
  'gps_kataster_obiektow_tatr.validator.x__is_number*' \
  'gps_kataster_obiektow_tatr.pig_staging.x__parse_pig_point*' \
  'gps_kataster_obiektow_tatr.pig_staging.x_write_staging_files*' \
  'gps_kataster_obiektow_tatr.tpn_staging.x__parse_tpn_point*' \
  'gps_kataster_obiektow_tatr.tpn_staging.x__load_existing_candidates*' \
  'gps_kataster_obiektow_tatr.tpn_staging.x__load_pig_staging_candidates*' \
  'gps_kataster_obiektow_tatr.tpn_staging.x_build_tpn_staging*' \
  'gps_kataster_obiektow_tatr.tpn_staging.x_write_staging_files*' \
  'gps_kataster_obiektow_tatr.source_profile.x__profile_numeric_column*' \
  'gps_kataster_obiektow_tatr.source_profile.x_write_report_files*' \
  'gps_kataster_obiektow_tatr.staging_review.x_write_review_report_files*'
```

Obejście nie modyfikuje projektu, zależności ani warunków zaliczania testów.
Wszystkie wcześniejsze wyniki -11 zostają zastąpione rzeczywistym retry;
nie wliczamy ich do zabitych mutantów.

Bramka po testach granic i dokumentacji:
`build/verification/run-2r67z829/report.json`, **exit 0, 683 passed**,
wszystkie etapy/readback 7/7, dane i liczniki niezmienione.
Hash drzewa: `f68de49d1e72ca053e6109f7031dae45828769b6dd08096d175e0f3fe5e185bc`.


Podczas domykania ścieżki review odtworzono również OverflowError przy
`record_number: .inf`/`-.inf` w decyzji lub wierszu stagingu: 4 wyjątki
przed poprawką. `_parse_positive_int` teraz obsługuje OverflowError tak jak
ValueError/TypeError; istniejąca diagnostyka odmawia zapisu. NaN już był
obsługiwany przez ValueError. Regresje obejmują obie lokalizacje i trzy
wartości, z porównaniem wszystkich bajtów katalogu. Nowy helper także
zostanie sprawdzony mutacyjnie.


Finalna logika z obsługą record_number: niezależny delta review bez uwag,
**303 passed** w testach finite_numbers/staging_review/review_validation.
Bramka `build/verification/run-ju8hil1p/report.json`: **exit 0, 705 passed**,
Ruff, wszystkie testy, walidacja, build i 7/7 readback poprawne.
Hash drzewa: `88d46899b22686dea04b42769beeaba9fe81bf94db576970939138e38fa81fe7`.
Hash data/, liczniki i ostrzeżenia identyczne z poprzednimi bramkami.


## Końcowy wynik mutacji

Pierwsza poprawna kampania: **1332 mutanty, 757 zabitych, 575 ocalałych**,
bez awarii/timeoutów/braku testów. Wygenerowanych 6623 w UI oznacza cały
zakres repo, nie liczbę uruchomionych mutantów.

Po analizie AST i niezależnym review dodano regresje kontynuacji po błędnym
wierszu TPN, identyfikatorów źródłowych, kodów/severity, projekcji, granicy
tolerancji, ochrony JSON oraz numeru rekordu. Świeże statystyki (usunięcie
mutants/mutmut-stats.json przed retry) objęły zmienione testy.
Ponowiono 172 ocalałe identyfikatory związane z tymi testami oraz 4 mutanty
`staging_review.x__parse_positive_int*`: **102 killed, 72 survived, 2 timeout**.
Dwa timeouty (`coordinates.x_wgs84_to_1992__mutmut_3` i `_4`) usuwały guardy
przed natywną projekcją. Dodano natychmiastowe asercje, że błędne wejście
nie dociera do transformera; ich retry wraz z
`numeric.x_require_finite_numbers__mutmut_3`: **3 killed, 0 timeout**.
Użyto tego samego obejścia setproctitle i `mutmut run --max-children 2`
z jawnymi identyfikatorami. Kod/testy nie są zastępowane mutantami w src/.

Łącznie **1336 unikalnych mutantów: 862 zabite, 474 przeanalizowane ocalałe**.
Końcowy zakres nie ma timeoutów, awarii ani mutantów bez testów. Wyniki retry
zastępują wcześniejsze dla tych samych ID; SIGSEGV nie wliczono do kampanii.
[Pełne grupy i identyfikatory ocalałych](PBI-042-mutations.md) są w repo.
Nie deklarujemy 100% pokrycia. Starsze luki dopasowania/metadanych/ostrzeżeń
pozostają jawne do PBI-054; guardy finite i dodatkowe blokady JSON są chronione.

## Dostarczenie i kontynuacja

Końcowa bramka `build/verification/run-y3ygd93h/report.json`: **exit 0,
707 passed**, Ruff format/lint, walidacja, build oraz readback 7/7 poprawne.
Hash drzewa: `1cd63c1a5860c34d84277b9d0e69b23d370dd298903b7819309c8b37b6db7c5a`.
Hash data/ przed/po, liczniki i ostrzeżenia identyczne z bramką wstępną.
Testy PBI-042: 183 przypadki. Brak zmian data/, schema/ i uv.lock.
Cały diff przeczytany; git diff --check poprawny.
Po końcowym uzupełnieniu dokumentacji ponowiono bramkę:
`build/verification/run-m61r3p3c/report.json`, exit 0, 707 passed, 7/7 readback.
Hash drzewa `f4b2139a609fa3dac5efd5c1d8dc4ee9b42a92117daea6159754b68d9b233e8a`;
data/, liczniki i ostrzeżenia niezmienione. `uv sync --frozen` i
`git diff --check` exit 0. Scoped commit obejmuje 20 plików PBI-042.

Commit kodu: `796e169e5de1c2bc3883442b681fdbd774985674`
(`fix: reject non-finite measurement values (PBI-042)`). Push na
`origin/codex/review-remediation` zakończony sukcesem; `git ls-remote`
zwróciło identyczny SHA. CI validate
[35449507147](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/35449507147):
**completed/success**, headSha identyczny z commitem kodu.
Adnotacje CI o wersjach Node akcji i przyszłej migracji ubuntu-latest nie
są błędami projektu; nie rozszerzano zakresu PBI o migrację workflow.

Checkpoint dokumentacji ustawia wykonane, zapisuje dostarczenie kodu i
następny krok. Bramka checkpointu `build/verification/run-bn9gl440/report.json`:
**exit 0, 707 passed, 7/7 readback**, data/ i wszystkie liczniki bez zmian.
Hash drzewa: `76cd5c29565fe36f732f74b0569930085a0adccc9b2ea343a3cdf92eb36c8c67`.
Po bramce dopisano wyłącznie jej wynik w tym logu. Publikację i CI samego
checkpointu potwierdza Git/origin oraz następna sesja,
bez dopisywania commitu zawierającego własny SHA.
Następne gotowe **PBI-043**, zależność PBI-040 dostarczona. Nie rozpoczynać
PBI-043 w tej sesji. Osobny PBI-055 opisuje duże skończone int w SQLite.
