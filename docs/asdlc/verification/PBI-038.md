# PBI-038 — Symetria obiekt–jaskinia

Data: 2026-09-19
Branch: `codex/review-remediation`
Bazowy SHA: `7b9cb486b96ca00f07550c5e6ef423171c0d1f73`
Status merytoryczny: wykonane
Ustalenie przeglądu: R03

## Zakres i decyzje

- Fetch i pull ff-only: HEAD = origin, 0/0, czysty checkout. PBI-037
  dostarczone; CI checkpointu
  [35424570458](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/35424570458)
  completed/success dla bazowego SHA. Zależność spełniona.
- Nowa kontrola walidatora porównuje oba kierunki: object.cave_id →
  cave.object_ids oraz cave.object_ids → object.cave_id. Kody:
  `OBJECT_CAVE_MEMBERSHIP_MISSING`, `CAVE_OBJECT_MEMBERSHIP_MISMATCH`.
  Sprzeczne wskazanie tego samego obiektu przez dwie jaskinie jest błędem
  drugiego kierunku; nie wymaga osobnej równoległej reguły.
- Grupowanie po ID zachowuje wszystkie rekordy, także duplikaty. Brak
  celu nadal zgłasza istniejąca kontrola referencji. Błędne typy zgłasza
  schema; brak/null cave_id oraz pusta jaskinia pozostają dozwolone.
- `link_cave` usuwa wpis ze starej jaskini i oznacza ją do zapisu wraz
  z nową jaskinią i obiektem; aktualizuje audyt wszystkich trzech.
  A→A zachowuje kolejność listy i nie tworzy duplikatu. ID, pomiary,
  best, referencje i pozostałe pola pozostają bez zmian.
- Kontrakt zapisano w specyfikacji §11.1 i opisie decyzji review.
  Nowy plik testów włączono do mutmut i jego testu konfiguracji.
- Pełna bramka wykryła istniejący fixture testu metadata eksportu:
  usuwał cave_id obiektu, lecz zostawiał go na liście jaskini. Poprawiono
  fixture na pustą listę; wszystkie dotychczasowe asercje i liczniki
  pozostały bez zmian. Pierwszy przebieg: 341 passed, 1 failed;
  nie był uznany za zaliczoną bramkę.
- Dane katalogu, schematy i lockfile bez zmian. Zachowanie `.yaml`
  i oryginalnych ścieżek (PBI-039), pełna walidacja review (PBI-040)
  oraz odporność na błędy I/O (PBI-041) pozostają osobnymi zadaniami.
  Review zaakceptował te granice; nie są zamykane przez ten PBI.

## Dowody weryfikacji

| Sprawdzenie | Komenda lub test | Wynik |
|---|---|---|
| Reprodukcja na bazowym kodzie | `uv run --frozen pytest -q tests/test_cave_membership.py --tb=no` | exit 1: 7 failed, 5 passed; cztery niespójności zaakceptowane, trzy scenariusze przeniesienia zostawiają starą przynależność |
| Pierwsza regresja po poprawce | testy cave_membership, validator, staging_review, identifier_scopes | exit 0: 87 passed |
| Pełna bramka po poprawie fixture metadata | `uv run --frozen python scripts/verify_project.py` | exit 0: 342 passed; Ruff, walidacja, build i readback 7 artefaktów |
| YAML + SQLite | `test_link_cave_persists_symmetric_membership_and_preserves_history` | A→B, A→A, A→B→C, obiekt nieprzypisany; pełne rekordy YAML, dry-run i bezpieczny retry, SQLite integrity/FK, oba kierunki przynależności i pomiary |
| Błędna przynależność | `test_inconsistent_membership_blocks_validation_and_sqlite` | cztery przypadki dają oczekiwany kod/plik/ID; istniejący SQLite i wejście bez zmiany bajtów |
| Testy po analizie mutacji | `uv run --frozen pytest -q tests/test_cave_membership.py` | exit 0: 21 passed; dodatkowo błędne/brakujące cele, kontynuacja po cave_id null/list oraz brak zapisów przy błędnej drugiej decyzji |
| Niezależny review | agent `review_pbi038`, tylko odczyt, cały diff i nowe pliki | brak ustaleń P0–P3; niezależnie 91 passed, w tym zmieniony fixture metadata |

Zaliczony raport przed rozszerzeniem testów:
`build/verification/run-_lrvgdtz/report.json`, exit 0.
Hash drzewa przed/po:
`e4488557df99d2a3dcb299dddf1a82b88f5bd7de91b1a68ddff742963ffbd7e1`.
Hash całego `data/` przed/po:
`8f3ca9fe15c9cd3f35a6ade5673004a529c93376f1738608cbf8a606784bb45b`.

Katalog: **1009 obiektów, 1003 jaskinie, 1875 pomiarów, 0 relacji**.
Walidacja: **0 błędów, 2066 ostrzeżeń**: 1875
`MISSING_HORIZONTAL_ACCURACY`, 103 `MEASUREMENT_OUTSIDE_VALLEYS`,
72 `MEASUREMENT_DISTANCE_OUTLIER`, 16 `OBJECT_PREFIX_MISMATCH`.
Python 3.12.13, pytest 9.0.3, Ruff 0.15.13, mutmut 3.5.0, PyYAML 6.0.3,
pyproj 3.7.2, pyshp 3.0.3. `uv sync --frozen` exit 0. Użyto
`UV_CACHE_DIR=/tmp/gps-kataster-uv-cache`; odbudowa lokalnego pakietu po
zmianie pyproject wymagała dostępu do PyPI poza sandbox DNS. Nie zmieniono
zależności ani lockfile; nie zaliczono nieudanej próby sieciowej jako testu.

### Mutacje zmienionych funkcji

```bash
uv run --frozen mutmut run --max-children 2 \
  'gps_kataster_obiektow_tatr.validator.x_validate_dataset*' \
  'gps_kataster_obiektow_tatr.validator.x__validate_cave_memberships*' \
  'gps_kataster_obiektow_tatr.staging_review.x__apply_link_cave*'
uv run --frozen mutmut results
```

Pierwszy przebieg: 237 mutantów, 173 zabite i 64 ocalałe, bez timeoutów,
wyników podejrzanych i bez testów w wybranym zakresie. Analiza wykryła
lukę continue→break po nieprzypisanym obiekcie oraz brak regresji dla
brakujących celów (zmiana domyślnego [] na None powodowała crash).
Dodano 9 testów i asercje pól raportu decyzji. Kod produkcyjny bez zmiany.
Powtórzenie całego wybranego zakresu: **237 mutantów: 217 zabitych,
20 ocalałych, 0 bez testów, timeoutów lub wyników podejrzanych**, exit 0.
`mutmut results` exit 0. Wszystkie mutanty uszkadzające naprawianą
symetrię, przeniesienie, zachowanie danych i kontynuację walidacji są zabite.

Analiza wszystkich 20 ocalałych (prefiks `gps_kataster_obiektow_tatr.`):

- `validator.x_validate_dataset__mutmut_18/19/20/23/24/25/28/29/30/34`:
  etykiety encji przy wcześniejszej kontroli duplikatów (XX, wielkość liter,
  None). Odrzucenie, kod, ścieżki i ID pozostają; nie wymuszamy pełnej frazy.
- `validator.x_validate_dataset__mutmut_47`: `repo_root=None` w starszej
  kontroli załączników. Wybrane testy nie obejmują lokalnego załącznika
  względnego; luka już opisana w PBI-037 (wtedy numer 45). Nie narusza
  przynależności; szersza kampania krytycznych modułów należy do PBI-054.
- `validator.x__validate_cave_memberships__mutmut_59/60/61`: błędny klucz
  tylko przy wyświetlaniu faktycznego cave_id w końcówce opisu. Reguła,
  kod, ścieżki oraz ID obiektu i jaskini zgłaszającej błąd pozostają.
- `staging_review.x__apply_link_cave__mutmut_25/26`: formatowanie opisu
  brakujących pól decyzji, bez zmiany kodu, severity i blokady zapisu.
- `staging_review.x__apply_link_cave__mutmut_67/69/85/87`: domyślne []
  zmienione na None lub usunięte dla braku pola object_ids w starej/nowej
  jaskini. Schema wymaga tego pola; dla poprawnych jaskiń mutanty są
  równoważne. Walidacja niepoprawnego wejścia review należy do PBI-040.

Niezależny review dodatkowych 9 przypadków i asercji raportu:
`review_pbi038`, brak uwag; niezależnie 21 passed w 0.79 s.
`git diff --check` oraz `git diff --exit-code HEAD -- data schema uv.lock`:
exit 0. Końcowa pełna bramka po zapisaniu tego logu obowiązuje przed commitem.

## Dostarczenie

Końcowa pełna bramka po zapisaniu dowodów i rozszerzeniu regresji:
`build/verification/run-2vzn9ut_/report.json`, **exit 0, 351 passed**,
Ruff format/lint, walidacja, build i readback wszystkich 7 artefaktów.
Hash drzewa przed/po:
`278de0fafeec7d8896f42a69e89511728cd0338e5f2d68c53e6356049299b39c`.
Hash data/ i wszystkie liczniki są takie same jak podano wyżej.
`git diff --check` i kontrola braku zmian data/schema/uv.lock: exit 0.

Commit implementacji: `8729c88158756efb1a0e4ed6617d24c554943e68`
(`fix: preserve cave membership consistency (PBI-038)`).
Push zakończył się exit 0; `git ls-remote --heads` odczytał dokładnie
powyższy SHA gałęzi `refs/heads/codex/review-remediation`.

Podczas dostarczenia SSH zwróciło `Permission denied (publickey)`.
Odczyt przez GitHub CLI i bezpośredni HTTPS działał. Push, ls-remote
oraz aktualizację lokalnej referencji origin wykonano do tego samego
repozytorium przez HTTPS, bez zmiany konfiguracji remotes:

```bash
git -c credential.helper= -c 'credential.helper=!gh auth git-credential' \
  push https://github.com/dlubom/gps-kataster-obiektow-tatr.git \
  HEAD:refs/heads/codex/review-remediation
git -c credential.helper= -c 'credential.helper=!gh auth git-credential' \
  ls-remote --heads https://github.com/dlubom/gps-kataster-obiektow-tatr.git \
  refs/heads/codex/review-remediation
```

CI `validate`: [run 35439112805](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/35439112805),
trigger push, SHA `8729c88158756efb1a0e4ed6617d24c554943e68`,
**completed/success**. Job `Validate code and data` ukończył wszystkie
kroki w 1 min 39 s; wynik potwierdzono przez `gh run view`.

Ten wpis jest osobnym checkpointem dokumentacji po dostarczeniu kodu.
Przed commitem checkpointu ponownie wykonujemy pełną bramkę.
Publikację i CI samego checkpointu potwierdzamy z Git/origin, bez
zapisywania jego własnego SHA w nim samym.

## Kontynuacja

PBI-038 dostarczone. Następne gotowe **PBI-039** (rozszerzenia YAML
oraz ścieżki aktualizacji); zależność PBI-037 jest dostarczona.
W nowej sesji wykonać fetch/pull ff-only, sprawdzić SHA i CI checkpointu,
a następnie wykonać tylko PBI-039. Nie rozpoczynać go w tej sesji.
Bez merge, tagu i publikacji release.
