# PBI-037 — Unikalność ID we właściwych zakresach

Data: 2026-09-18
Branch: `codex/review-remediation`
Bazowy SHA: `f0eec502844853c3005f6f35443eef3a6ed82aca`
Status merytoryczny: zweryfikowane — do dostarczenia
Ustalenie przeglądu: R04

## Zakres i decyzje

- Fetch, pull ff-only i porównanie historii potwierdziły HEAD = origin
  (0/0). PBI-036 i checkpoint są dostarczone; CI checkpointu:
  [run 34136322675](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/34136322675),
  completed/success dla bazowego SHA. Zależność PBI-037 jest spełniona.
- Walidator kontroluje globalną unikalność ID osobno dla obiektów,
  jaskiń i relacji, zachowując wszystkie wystąpienia oraz ich ścieżki.
  Kontrola lokalnych ID pomiarów i załączników poprzedza tworzenie zbiorów
  referencji; zbiory wykrytych ID są osobne dla każdego pola i obiektu.
- Identyczne wpisy są także duplikatami. Identyczne załączniki już wcześniej
  odrzucało schema uniqueItems; teraz dodatkowo mają błąd domenowy ID.
  Różne załączniki z tym samym ID także są odrzucane.
- Kody: `DUPLICATE_OBJECT_ID`, `DUPLICATE_CAVE_ID`, `DUPLICATE_RELATION_ID`,
  `DUPLICATE_MEASUREMENT_ID`, `DUPLICATE_ATTACHMENT_ID`. Diagnostyka zawiera
  plik, ID i zakres. Te same m-001/a-001 w dwóch obiektach są poprawne.
- Build SQLite i eksport używają już wspólnego walidatora. Nie wymagały
  zmiany: nowe błędy zatrzymują je przed zapisem lub indeksowaniem danych.
- Kontrakt zapisano w specyfikacji §11.1. Dodano testy do selekcji mutmut;
  test CLI w podprocesie jest wyłączony tylko z mutmut, tak jak istniejące
  testy subprocess. Pełny pytest nadal uruchamia całą macierz CLI.
- Dane, schematy i lockfile są niezmienione. PBI-038+ oraz materializacja
  review pozostają poza zakresem tego zadania.

## Dowody weryfikacji

| Sprawdzenie | Komenda lub test | Wynik |
|---|---|---|
| Pierwsza reprodukcja przed poprawką | `pytest -q tests/test_identifier_scopes.py --tb=no` | exit 1: 40 failed, 11 passed; brak czterech kontroli domenowych, błędy SQLite lub akceptacja eksportu |
| Finalne regresje na bazowym kodzie | kopia `git archive f0eec502844853c3005f6f35443eef3a6ed82aca` + aktualny plik testów, Python z frozen venv; `pytest -q --tb=no -o pythonpath=src tests/test_identifier_scopes.py` | exit 1: 44 failed, 12 passed |
| Regresje po poprawce | `uv run --frozen pytest -q tests/test_identifier_scopes.py` | exit 0: 56 passed |
| Zakresy ID | testy parametryzowane 5 rodzajów ID, wpisy identyczne i różne, `.yml`/`.yaml` | błędy domenowe, poprawne ścieżki i ID; unikalny wcześniejszy rekord nie ukrywa późniejszego duplikatu |
| Ochrona artefaktów | `test_cli_duplicate_ids_preserve_artifacts`, 5 × 4 CLI × 2 stany wyjścia | validate/build/export/release exit 1 bez tracebacku; bajty całego katalogu wejścia i wyjścia identyczne, bez nowych plików |
| Poprawne lokalne ID | `test_local_ids_can_repeat_in_different_objects_and_build` | SQLite integrity/FK poprawne; oba obiekty zachowują m-001 i a-001; 7 artefaktów, 2 rekordy GeoJSON |
| Błędny typ ID | `test_invalid_local_id_does_not_hide_later_duplicates` | schema error bez crasha oraz osobny błąd późniejszego duplikatu |
| Pełna bramka po rozszerzeniu testów | `uv sync --frozen`; `uv run --frozen python scripts/verify_project.py` | exit 0: 330 passed; Ruff format/lint, walidacja katalogu, build i readback 7 artefaktów |
| Źródła | hash `data/` przed/po i `git diff --exit-code HEAD -- data schema uv.lock` | identyczne, exit 0 |
| Niezależny review | agent `review_pbi037`, tylko odczyt, cały diff i nowe pliki | brak ustaleń; niezależnie 51 testów, potem 5 dodatkowych testów po mutacjach; delta zaakceptowana |
| Diff | odczyt całego diffu i `git diff --check` | zakres PBI-037, exit 0 |

Weryfikacja początkowo wykryła brak aktualizacji testu konfiguracji mutmut
oraz format/lint; poprawiono przed zaliczeniem bramki. Jeden przebieg,
którego konfigurację zmieniono w trakcie, poprawnie zakończył się exit 1
(kontrola niezmienności drzewa), mimo poprawnych etapów. Za dowód zaliczenia
służy dopiero poniższy przebieg bez zmian w trakcie.

Raport po review i uzupełnieniu testów:
`build/verification/run-gny2v9og/report.json`, exit 0.
Hash drzewa przed/po: `bfeebf9086dabf276a928c58af0d0d32bc5448f2808523dbed039997ab65c8fb`.
Hash całego `data/` przed/po:
`8f3ca9fe15c9cd3f35a6ade5673004a529c93376f1738608cbf8a606784bb45b`.
Przed commitem obowiązuje ponowna pełna bramka po uzupełnieniu dokumentacji.

Katalog: **1009 obiektów, 1003 jaskinie, 1875 pomiarów, 0 relacji**.
Walidacja: **0 błędów, 2066 ostrzeżeń**: 1875
`MISSING_HORIZONTAL_ACCURACY`, 103 `MEASUREMENT_OUTSIDE_VALLEYS`,
72 `MEASUREMENT_DISTANCE_OUTLIER`, 16 `OBJECT_PREFIX_MISMATCH`.
Python 3.12.13, pytest 9.0.3, Ruff 0.15.13, mutmut 3.5.0, PyYAML 6.0.3,
pyproj 3.7.2, pyshp 3.0.3. Frozen sync bez zmiany zależności. Po zmianie
pyproject pobranie build backendu wymagało dostępu poza sandbox DNS;
nie potraktowano tego jako błędu projektu.

### Mutacje zmienionych funkcji

```bash
uv run --frozen mutmut run --max-children 2 \
  'gps_kataster_obiektow_tatr.validator.x_validate_dataset*' \
  'gps_kataster_obiektow_tatr.validator.x__validate_duplicate_record_ids*' \
  'gps_kataster_obiektow_tatr.validator.x__validate_duplicate_local_ids*'
uv run --frozen mutmut results
```

Obie komendy exit 0. **103 mutanty: 89 zabitych, 14 ocalałych,
0 bez testów, timeoutów i podejrzanych wyników** w tym zakresie.
Pierwszy pomiar: 87 zabitych, 16 ocalałych. Dwa przypadki continue→break
(globalne ID po unikalnym rekordzie i lokalne ID po błędnym typie) wykazały
luki testów; dodano 5 regresji i ponowiono wszystkie 103 mutacje.
Oba mutanty są teraz zabite. Produkcyjna logika nie wymagała zmiany.

Analiza 14 ocalałych (prefiks `gps_kataster_obiektow_tatr.validator.`):

- `x_validate_dataset__mutmut_18/19/20/23/24/25/28/29/30/34`: zmiany
  etykiety rodzaju encji w opisie (wielkość liter, XX, None), bez zmiany
  odrzucania, kodu, ścieżek ani ID. Nie wymuszamy pełnego tekstu komunikatu.
- `x_validate_dataset__mutmut_45`: `repo_root=None` przy istniejącej
  walidacji załączników; wybrane testy używają URL i nie przechodzą przez
  względną ścieżkę lokalną. To luka testów wcześniejszej reguły ścieżek,
  nie naruszenie naprawianej unikalności. Produkcyjny argument pozostaje.
- `x__validate_duplicate_record_ids__mutmut_8`: zmienia separator listy
  ścieżek na XX; wszystkie ścieżki i odrzucenie pozostają.
- `x__validate_duplicate_local_ids__mutmut_30/31`: zmiana końcowej frazy
  opisu; kod, ścieżka, ID i odrzucenie pozostają.

Mutmut początkowo nie zebrał statystyk dla testu subprocess (brak kontekstu
mutmut w procesie CLI); wynik niezaliczony. Wyłączenie tylko tego testu
z selekcji mutacji umożliwiło powyższe pełne pomiary. Pozostałe testy
regresji w tym samym pliku mierzą krytyczne reguły w procesie mutmut.

## Dostarczenie

Oczekuje na commit/push po końcowej bramce dokumentacji i sprawdzenie CI.

## Kontynuacja

Po dostarczeniu PBI-037 następne gotowe jest **PBI-038** (symetria
obiekt–jaskinia i link_cave), zależne od PBI-037. W nowej sesji sprawdzić
origin i CI checkpointu, a następnie wykonać tylko PBI-038. Nie rozpoczynać
go w tej sesji. Bez merge, tagu i publikacji release.
