# PBI-034 — Plan napraw i kontynuacja z czystej sesji

Data: 2026-09-05
Branch: `codex/review-remediation`
Bazowy SHA: `a3dd1a671d3d93cd6958242fc68ddcf1d37cf846`
Status merytoryczny: zweryfikowane — do dostarczenia
Ustalenia: R01–R13, wyłącznie planowanie napraw.

## Zakres i decyzje

- Utrwalono raport i krótkie logi przeglądu w `docs/asdlc/reviews/`.
  Odnośniki są przenośne; dalsza praca nie wymaga historycznego `build/`
  ani katalogu tymczasowego przeglądu.
- Podział na PBI-035–054 obejmuje wspólną bramkę, poprawki kontraktów,
  import/review, eksporty, dowody dla danych i końcowy review.
- Dodano instrukcję pojedynczej sesji, wzór logu, statusy i punkt startu
  w AGENTS/backlog/context. Jeden branch, częste zweryfikowane checkpointy,
  bez merge/tagu/release w zakresie serii.
- Rozdzielono techniczne ID assignment od danych z 16 rozbieżnościami,
  aby reguła wymagająca powodów nie psuła pośrednich checkpointów.
- Konsultacja niezależnego agenta wykryła ryzyko redukcji wejścia po ID
  przed walidacją oraz konieczność zachowania relacji i ścieżek. Włączono
  je do PBI-040. Pełny review agenta nie zakończył się z powodu limitu
  dostępności; nie jest traktowany jako aprobata całego planu.
- Kod aplikacji, finalne dane i ich schematy pozostają bez zmian.

## Dowody weryfikacji

Nowy przebieg dla PBI-034, Python 3.12.13:

| Sprawdzenie | Wykonanie | Wynik |
|---|---|---|
| Zależności | `uv sync --frozen --offline`, cache w `/private/tmp` | exit 0, 30 pakietów |
| Dokumenty | linki, 21 kart PBI-034–054, zależności wskazują wcześniejsze PBI, pokrycie R01–R13 | 10 plików sprawdzonych, 0 błędów |
| Format | `ruff format --check src tests scripts` | exit 0, 45 plików |
| Lint | `ruff check src tests scripts` | exit 0 |
| Testy | `pytest -q` | 123 passed in 1.69s |
| Dane | `python scripts/validate.py` | exit 0, 0 błędów, 2066 ostrzeżeń |
| Release | `python scripts/build_release_artifacts.py --sqlite-output build/verification/PBI-034-run1/katalog.sqlite --output-dir build/verification/PBI-034-run1/exports --generated-at 2026-09-05T12:00:00Z` | exit 0, 7 artefaktów |
| Readback | niezależny odczyt YAML, sqlite3, csv, json, ElementTree, zipfile, pyshp | integralność/FK, oba kierunki cave/object, liczniki, ID pomiarów, osie i współrzędne wszystkich formatów zgodne |
| Dane przed/po | SHA-256 nazw ścieżek i zawartości całego `data/` | identyczne |
| Diff | `git diff --check`, lokalny przegląd zakresu | exit 0; tylko dokumentacja i dowody |
| Mutacje | dokumentacyjny PBI, bez zmian logiki | nie dotyczy |

Liczniki: 1009 obiektów, 1003 jaskinie, 1875 pomiarów, 0 relacji.
Ostrzeżenia: `MISSING_HORIZONTAL_ACCURACY` 1875,
`MEASUREMENT_OUTSIDE_VALLEYS` 103, `MEASUREMENT_DISTANCE_OUTLIER` 72,
`OBJECT_PREFIX_MISMATCH` 16. Bez zmian względem punktu wyjścia.
Hash całego `data/` przed i po:
`0f43372a8ae059fba95162a974d3941c1d7614879e3c76ad77bc200d1dc858f2`.
Pełne lokalne wyniki: `build/verification/PBI-034-run1/` (generowane;
kontynuacja opiera się na powyższych zapisanych wynikach i ponownym przebiegu).
Nie było kodu do testu red/green; PBI utrwala plan, nie naprawia R01–R13.

## Dostarczenie

Commit planu: do utworzenia po weryfikacji.
Push: do wykonania i potwierdzenia.
CI: nie dotyczy dla PBI-034 — obecny workflow nie wyzwala się na tym
branchu bez PR; zmiana triggera jest zakresem PBI-035.

## Kontynuacja

Po dostarczeniu PBI-034: PBI-035, jedna odtwarzalna bramka lokalna/CI.
Ta sesja przygotowuje plan; nie rozpoczyna implementacji PBI-035.
Przy przerwaniu najpierw sprawdzić Git i domknąć dostarczenie planu.
