# PBI-035 — Pełna odtwarzalna bramka lokalna i CI

Data: 2026-09-05–06
Branch: `codex/review-remediation`
Bazowy SHA: `7f6b4a8eaea1ec1a052c5c11744c097a160dd858`
Status merytoryczny: wykonane

## Zakres i decyzje

- PBI-034 i jego checkpoint potwierdzone po `git fetch origin` oraz
  `git pull --ff-only`; HEAD i origin zgodne, rozbieżność 0/0.
  Po wznowieniu 2026-09-06 ponowny fetch także potwierdził 0/0.
- `scripts/verify_project.py` uruchamia Ruff format/lint, cały pytest,
  walidację katalogu, build w nowym katalogu i readback 7 artefaktów.
  Raport zawiera komendy, kody wyjścia, wersje, HEAD, hash drzewa,
  hash całego `data/`, liczniki, ostrzeżenia i sumy artefaktów.
- Pierwszy nieudany etap blokuje kolejne. Kontrola niezmienności wejścia
  działa również po błędzie. Każdy przebieg ma własny pusty katalog;
  udane stare pliki nie zastępują brakującego wyniku nowego buildu.
- Odczyt porównuje ID/liczniki i wskazany w YAML pomiar (także `manual`),
  współrzędne i osie, oba kierunki członkostwa jaskiń, integralność/FK
  SQLite, metadane i CRC ZIP. Wszystkie pola DBF są dekodowane jako UTF-8.
  Indeks SHX jest porównywany z odczytem sekwencyjnym geometrii SHP.
- Istniejący ESRI WKT ma alias datum bez identyfikatora EPSG; kontrola
  projekcji porównuje rzeczywiste przeliczenie punktów z EPSG:2180,
  zamiast wymagać identycznej nazwy CRS. Nie zmieniono eksportera.
- `validate.yml` zachowuje PR/main, dodaje push na gałąź naprawczą oraz
  `uv sync --frozen` i wspólny runner. Bez publikacji artefaktów/release.
- Dokumentacja operacyjna, specyfikacja CI i runbook opisują nową bramkę.
  Kod domenowy, lockfile, dane i schematy pozostają bez zmian. Naprawy
  R01–R13 i PBI-036 nie są wykonywane w tym PBI.

## Dowody weryfikacji

| Sprawdzenie | Komenda lub test | Wynik |
|---|---|---|
| Reprodukcja na bazowym kodzie | `pytest -q tests/test_ci_workflow.py tests/test_verify_project.py` po dodaniu oczekiwań | exit 1: 2 failed, 4 passed; brak runnera oraz trigger tylko `main` |
| Regresje runnera i CI | `pytest -q tests/test_verify_project.py tests/test_ci_workflow.py` | exit 0: 52 passed po poprawce SHX |
| Awaria etapów | parametryzacja wersje/format/lint/testy/walidacja/build/readback | niezerowy wynik i zachowany kod; brak dalszych etapów |
| Świeżość i wejście | drugi build niczego nie zapisuje; mutacja kodu/danych podczas bramki; hash nowych plików | stare artefakty odrzucone; zmiana wejścia daje błąd |
| Readback fixture | auto/manual o różnych współrzędnych, wiele otworów, pusta jaskinia, obiekt bez jaskini, relacja, null wysokości | poprawna ścieżka i awarie każdego z 7 artefaktów |
| Uszkodzenia | ID/duplikaty/best, osie, metadane, członkostwo, relacja, SQLite/FK, ZIP/CRC, SHP/SHX/UTF-8 | wszystkie wykrywane |
| Ręczna bramka wdrożenia | osobno frozen sync, Ruff format/lint, pytest, validate, build, readback | wszystkie exit 0; 169 testów przed dodatkowym testem SHX |
| Pełny runner przed review | `uv run --frozen python scripts/verify_project.py` | exit 0, 169 testów, poprawny build/readback i niezmienność źródeł |
| Niezależny review | agent `review_pbi035`, tylko do odczytu | P2 SHX naprawione, końcowy review: brak otwartych znalezisk; niezależnie 52 testy exit 0 |
| Mutacje | brak zmian krytycznych reguł/modułów z zakresu mutmut | nie dotyczy; wymuszanie awarii i uszkodzeń testuje samą bramkę |

Ręczne komendy wdrożenia (każdy exit code sprawdzony oddzielnie):

```bash
uv sync --frozen --offline
uv run --frozen ruff format --check src tests scripts
uv run --frozen ruff check src tests scripts
uv run --frozen pytest
uv run --frozen python scripts/validate.py
uv run --frozen python scripts/build_release_artifacts.py \
  --sqlite-output build/verification/PBI-035-manual/katalog.sqlite \
  --output-dir build/verification/PBI-035-manual/exports \
  --generated-at 2026-09-05T12:00:00Z
```

Readback ręcznego buildu: `readback_artifacts` z nowego skryptu,
korzystający z sqlite3/csv/json/ElementTree/zipfile/pyshp, zakończony exit 0.
Zależności: Python 3.12.13, uv 0.11.8; frozen sync sprawdził 30 pakietów,
Ruff 0.15.13, pytest 9.0.3, PyYAML 6.0.3, pyshp 3.0.3, pyproj 3.7.2.
W sandboxie polecenia miały `UV_CACHE_DIR=/private/tmp/gps-kataster-uv-cache`.
Nie zmieniono lockfile.

Dane: **1009 obiektów, 1003 jaskinie, 1875 pomiarów, 0 relacji**.
Walidacja: **0 błędów i 2066 ostrzeżeń**: 1875
`MISSING_HORIZONTAL_ACCURACY`, 103 `MEASUREMENT_OUTSIDE_VALLEYS`,
72 `MEASUREMENT_DISTANCE_OUTLIER`, 16 `OBJECT_PREFIX_MISMATCH`.
Bez zmiany względem przeglądu i PBI-034.

Pierwszy pełny raport: `build/verification/run-8ke6aa04/report.json`,
exit 0, hash drzewa przed/po
`68cee636f5c6932b4ea2b0c83a411f120a0c222d3cb62f2f619bce486bface7f`.
To historyczny przebieg przed poprawką SHX i uzupełnieniem niniejszego logu.
Hash całego `data/` przed/po:
`8f3ca9fe15c9cd3f35a6ade5673004a529c93376f1738608cbf8a606784bb45b`.
Algorytm obejmuje nazwy, zawartość i wykonywalność plików; różni się od
ad hoc hasha PBI-034, więc sumy między tymi algorytmami nie są porównywane.

Review wykrył brak odczytu indeksowego SHX: uszkodzony offset 1000000
z poprawnym CRC ZIP przechodził sekwencyjny odczyt. Nowy test
`test_readback_detects_corrupt_artifact[shx_offset]` przed poprawką:
1 failed (`DID NOT RAISE`), po poprawce: przechodzi. Początkowa próba
review 2026-09-05 przerwana przez limit narzędzia; skutecznie ponowiona
2026-09-06. Nie traktowano błędu narzędzia jako aprobaty.

Końcowa bramka po poprawce SHX:
`uv sync --frozen --offline` oraz
`uv run --frozen python scripts/verify_project.py`: exit 0;
**170 testów**, Ruff format/lint, walidacja i odczyt 7 świeżych artefaktów
poprawne. Raport `build/verification/run-f1b305z2/report.json`.
Hash `data/` jak wyżej; drzewo przed/po bez zmian podczas przebiegu.
`git diff --check`: exit 0; sprawdzono lokalne linki dokumentacji.
`git diff --exit-code HEAD -- data src schema uv.lock pyproject.toml`:
exit 0, brak nowych nieśledzonych plików w `data/`.

## Dostarczenie

Commit implementacji: `8f096455a491fbf00fdd21319e18372e09878b93`
(`test: add repeatable project verification (PBI-035)`).
Push: `git push origin codex/review-remediation`, exit 0.
`git ls-remote --heads origin refs/heads/codex/review-remediation` zwróciło
powyższy SHA, identyczny z lokalnym HEAD.

CI: workflow `validate`, run `34012763897`, trigger `push`,
HEAD `8f096455a491fbf00fdd21319e18372e09878b93`, **completed / success**;
`gh run watch --exit-status`: exit 0, job `Validate code and data` 58 s.
[Wynik CI](https://github.com/dlubom/gps-kataster-obiektow-tatr/actions/runs/34012763897).

Bramka bezpośrednio przed commitem, po uzupełnieniu dokumentacji:
`build/verification/run-d3wmdaf4/report.json`, exit 0, 170 passed in 5.29s,
Ruff/walidacja/build/readback bez błędów. Hash drzewa tego przebiegu:
`babcaa2977e3f6face7045fa00865c8bd2855fe2af499fee9260238ade524beb`.
Dane przed/po mają niezmieniony hash podany wyżej.

Niniejszy wpis jest osobnym checkpointem dokumentacji po dostarczeniu
implementacji. Jego publikację i CI następna sesja sprawdza względem Git;
nie umieszczamy SHA checkpointu w nim samym. Przed jego commitem obowiązuje
ponowna pełna bramka.

## Kontynuacja

PBI-035 dostarczone i CI zielone. Następne gotowe: **PBI-036**
(duplikaty kluczy YAML). Nie rozpoczynać go w tej sesji. Kontynuacja wymaga
wyłącznie plików śledzonych i sprawdzenia origin; lokalne raporty i eksporty
można odtworzyć. Bez merge, tagu i publikacji release.
